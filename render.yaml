services:
  - type: web
    name: optical-clinic-api
    env: java
    buildCommand: mvn clean install
    startCommand: java -jar target/*.jar
    plan: free
    buildFilter:
      paths:
        - pom.xml
    envVars: []
